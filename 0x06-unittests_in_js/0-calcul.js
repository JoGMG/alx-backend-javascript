/**
 * A function named `calculateNumber`. It should accepts
 * two arguments (number) `a` and `b`, rounds and returns
 * their sum.
 */
function calculateNumber(a, b) {
    result = a + b;
    return Math.ceil(result);
}

module.exports = calculateNumber;
